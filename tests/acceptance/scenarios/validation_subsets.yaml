# Scenario: Subset pattern detection
# Tests that redundant subset patterns are identified

description: "Detect patterns that are subsets of other patterns in same fact"

rules:
  has-subset:
    fact: "Has subset pattern"
    incl:
      - "p:src/**/*.js"  # subset of **/*.js
      - "p:**/*.js"      # superset

  chain-subset:
    fact: "Chain of subsets"
    incl:
      - "p:src/api/**/*.js"  # most specific
      - "p:src/**/*.js"      # middle
      - "p:**/*.js"          # most general

  no-subset:
    fact: "No subset relationship"
    incl:
      - "p:**/*.js"
      - "p:**/*.ts"  # different extension, not a subset

expected_warnings:
  subsets:
    has-subset:
      - ["p:src/**/*.js", "p:**/*.js"]
    chain-subset:
      - ["p:src/api/**/*.js", "p:src/**/*.js"]
      - ["p:src/api/**/*.js", "p:**/*.js"]
      - ["p:src/**/*.js", "p:**/*.js"]
    no-subset: []
